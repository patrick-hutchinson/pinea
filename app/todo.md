-> Delete voice type
-> Delete interview type
-> Rename periodical (on homepage, which is actually just a link to a visit)

the <Image /> component does not have a valid ref attached to it:
The only red is being passed from the shrinkmedia component,
-> CHECK: <Image /> now uses the forwarded ref if it exists, and if not, uses a fallback ref defined in the component?

Slideshow container updated, dass das bild nicht immer neu lädt
